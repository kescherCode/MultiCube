using System;

namespace MultiCube
{
    static class Globals
    {
        public const int MAX_SCREEN_COUNT = 10;
        public const float ZOOM_FACTOR = 3.2f;
        public const char H_BORDER_CHAR = '-';
        public const char V_BORDER_CHAR = '|';
        public const char CUBE_CHAR = '°';
        public static Random random = new Random();
    }
}
